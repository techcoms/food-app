apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: food-app-cluster
  region: ap-south-1
  version: "1.30"

managedNodeGroups:
  - name: standard-workers
    instanceType: t3.medium
    minSize: 2
    maxSize: 3
    volumeSize: 20
