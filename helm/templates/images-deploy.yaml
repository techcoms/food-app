{{- if .Values.images.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "food-app.fullname" . }}-images
spec:
  replicas: {{ .Values.images.replicas }}
  selector:
    matchLabels:
      app: {{ include "food-app.fullname" . }}-images
      component: images
  template:
    metadata:
      labels:
        app: {{ include "food-app.fullname" . }}-images
        component: images
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: images
          image: "{{ .Values.images.image.repository }}:{{ .Values.images.image.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.images.port }}
          volumeMounts:
            - name: uploads-dir
              mountPath: /data/uploads
          env:
            - name: MONGO_URI
              value: "mongodb://{{ include \"food-app.fullname\" . }}-mongo:27017/fooddb"
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.images.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.images.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: uploads-dir
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "food-app.fullname" . }}-images
spec:
  type: ClusterIP
  selector:
    app: {{ include "food-app.fullname" . }}-images
    component: images
  ports:
    - port: {{ .Values.images.port }}
      targetPort: {{ .Values.images.port }}
{{- end }}
